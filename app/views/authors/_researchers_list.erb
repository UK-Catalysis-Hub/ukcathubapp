<h5> Researcher Authoring UK Catalysis Hub Publications</h5>
<%= will_paginate authors, renderer: WillPaginate::ActionView::BootstrapLinkRenderer  %>
<table class="table">
  <thead class="thead-light">
    <tr>
      <th>Author</th>
      <th>Articles</th>
      <th>ORCID</th>
    </tr>
  </thead>
  <tbody>
    <% authors.each do |author| %>
      <tr>
        <td><%= link_to(author.full_name, author) %> </td>
        <th> <%= author.articles.count %> </td>
        <th> <%= author.orcid %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<br>
<% if user_signed_in? %>
  <%= link_to 'New Author', new_author_path, :class => "btn btn-success btn-sm"  %>
<% end %>
