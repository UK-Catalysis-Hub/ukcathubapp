<p style="color: green"><%= notice %></p>

<h1>Organisations</h1>

<div id="organisations">
  <%= render "organisations/organisations_list", organisations: @organisations, org_search: @search %>
</div>
<% if user_signed_in? %>
<%= link_to "New organisation", new_organisation_path %>
<% end %>
