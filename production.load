load database

     FROM sqlite://./storage/prod_migrate.sqlite3

     INTO postgresql://username:password@localhost:5432/db_production


 with include drop, create tables, create indexes, reset sequences

  set work_mem to '16MB', maintenance_work_mem to '512 MB';
